<%= simple_form_for(@plan, :html => { mulitpart: true }) do |f| %>
    <% if @plan.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
    <button type="button" class="close" data-dismiss="true">
      <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
    </button>
      <h4><%= pluralize(@delivery.errors.count, "error") %> prohibited this delivery from being saved:</h4>

      <ul>
      <% @plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <%= f.input :plan_name %>
    <%= f.input :plan_descrip %>
    <%= f.input :plan_info %>
    <%= f.input :plan_price %>
  </div>
  <div class="form-group">
    <%= f.label :plan_image %><br>
    <%= f.file_field :plan_image, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>